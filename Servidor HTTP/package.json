{
  "name": "servidor-http",
  "version": "0.0.1",
  "description": "Exemplo de como criar um servidor HTTP(Web API) sem usar framworks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
